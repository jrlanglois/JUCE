sudo: required
dist: trusty

language: cpp

git:
    depth: 5

before_install:
    - export DEBIAN_FRONTEND=noninteractive
    - sudo apt-get update --fix-missing
    - sudo apt-get install -qq
        clang
        libfreetype6-dev
        libx11-dev
        libxinerama-dev
        libxrandr-dev
        libxcursor-dev
        libxcomposite-dev
        mesa-common-dev
        libasound2-dev
        freeglut3-dev
        libcurl4-gnutls-dev+
        libasound2-dev
        libjack-dev
    - sudo apt-get update --fix-missing
    - sudo apt-get update -qq

compiler:
    - clang

branches:
    only:
        - master

script:
    - pwd
    - make -C examples/Demo/Builds/Linux
    - make -C extras/UnitTestRunner/Builds/LinuxMakefile
